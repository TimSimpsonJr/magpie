# Magpie dev + runtime dependencies — Layer 0–1 (laptop-local, no heavy Docker).
# Pinned to the versions validated during Phase 0 setup on Python 3.12.10.
# Later phases append: spacy + en_core_web_lg (Phase 5), docling/rapidocr/ocrmypdf
# (Phase 6), x-ray (Phase 7), mcp-sqlite (Phase 3). Keep this file the source of
# truth for the dev venv.

pytest==9.0.3
pandas==3.0.3
duckdb==1.5.3
pyarrow==24.0.0
numpy==2.4.6

# Phase 3.1 dirty-data loader (scripts/load_table.py):
#   openpyxl           — read .xlsx + unmerge merged cells (the XLSX path).
#   charset-normalizer — encoding pre-flight (sniff a byte sample so a latin-1
#                        FOIA export reads without mojibake). Versions verified
#                        against the Phase 3 research gate (prior-art.md §3, §5.1).
openpyxl==3.1.5
charset-normalizer==3.4.3

# Phase 3.4 dataset-analyze served-DB builder (scripts/build_dataset_db.py):
#   sqlite-utils — build + index the read-only SQLite DB that mcp-sqlite serves
#                  (TEXT-whitelist via explicit str columns before insert; FTS5
#                  on name cols). Version verified against the Phase 3 research
#                  gate (prior-art.md §2). mcp-sqlite itself runs via `uvx`, not
#                  pip, so it is NOT pinned here (see skills/dataset-analyze/.mcp.json).
sqlite-utils==3.39

# Test/validation-only: parse + validate the mcp-sqlite metadata
#   (skills/dataset-analyze/canned_queries.yml) and SKILL.md frontmatter in
#   tests/test_dataset_analyze_wiring.py (every canned query LIMIT-capped, no
#   write-enabled query). mcp-sqlite parses this YAML at runtime via `uvx`;
#   PyYAML is otherwise NOT a magpie runtime dependency.
PyYAML==6.0.2

# Phase 5 pii-sweep (scripts/pii_sweep.py) — the FIRST heavy ML dependency, CPU-only:
#   spacy           — NER engine. 3.8.14 verified (Phase 5 research gate) to install
#                     against the pinned numpy==2.4.6 WITHOUT touching it (numpy>=1.19.0
#                     is the only constraint), on Python 3.12.10; pulls thinc 8.3.13 +
#                     blis 1.3.3 (CPU BLAS). pii-sweep uses NER ONLY, disabling
#                     tagger/parser/lemmatizer/attribute_ruler so the live pipeline is
#                     just tok2vec+ner; cold model load ≈ 1.5 s on this box.
#   en_core_web_lg  — the trained pipeline (model v3.8.0, 400.7 MB wheel) pinned by its
#                     reproducible GitHub-release URL — the spaCy-recommended way to pin
#                     a model in a requirements file. PERSON is one of its 18 OntoNotes
#                     NER labels. The Simpsonville pilot validated `lg` specifically, so
#                     the Phase 11 golden PII tally is pinned to lg (NOT md/sm). NOTE:
#                     `mise run bootstrap` now downloads ~400 MB for this model.
spacy==3.8.14
en_core_web_lg @ https://github.com/explosion/spacy-models/releases/download/en_core_web_lg-3.8.0/en_core_web_lg-3.8.0-py3-none-any.whl

# Phase 6 ingest (scripts/ingest_gate.py, scripts/ingest.py) — the document/PDF
# path. HEAVIER than spaCy: `docling` pulls torch (CPU) + the layout/TableFormer
# model stack + RapidOCR (ONNX). Verified at the Phase 6 research gate
# (skills/ingest/references/prior-art.md) to install against the pinned
# numpy==2.4.6 + pandas==3.0.3 WITHOUT moving them, on Python 3.12.10. NOTE:
# `mise run bootstrap` now also fetches ~1.5-2 GB of wheels + first-run model
# weights (HuggingFace layout/TableFormer + the RapidOCR ONNX models), cached
# locally on first OCR.
#   docling      — DoclingDocument PDF understanding; the JSON is kept INTERNALLY
#                  (never Markdown) so page+bbox+charspan provenance survives for
#                  the Phase 8 citation anchor. `docling` is the convenience
#                  metapackage (== docling-slim[standard], which also pulls a
#                  chunking/transformers stack ingest does not use — a tracked
#                  footprint-reduction option, see prior-art §9).
#   rapidocr     — the OCR engine Docling 2.97 ACTUALLY uses (rapidocr>=3.8,<4);
#                  pure-Python + ONNX, NO system binaries. The legacy
#                  `rapidocr-onnxruntime` is a DIFFERENT distribution Docling 2.97
#                  does not import — deliberately NOT pinned.
#   onnxruntime  — CPU inference backend for RapidOCR.
#   torch        — pinned to the CPU build so a venv rebuild can't drift onto a
#                  CUDA wheel (docling-ibm-models layout/TableFormer runtime).
#   ocrmypdf     — deskew / --redo-ocr PREPROCESSING only. REQUIRES system
#                  binaries (Tesseract + Ghostscript) that pip does NOT manage;
#                  ingest detect-and-skips OCRmyPDF when they are absent
#                  (setup-doctor, Phase 10, installs them). The Python package is
#                  pinned so the seam + its MissingDependencyError stay
#                  importable/testable. Transitive deps (docling-core/-parse/
#                  -ibm-models, pikepdf, pdfminer.six, pypdfium2, fpdf2, img2pdf,
#                  onnx, opencv) are constrained by these top-level pins, not
#                  pinned individually (same policy as the spaCy thinc/blis stack).
docling==2.97.0
rapidocr==3.8.1
onnxruntime==1.26.0
torch==2.12.0
ocrmypdf==17.5.0

# Phase 7 redaction-check (scripts/redaction_check.py) — Free Law `x-ray` (BSD-2),
# the box-over-live-text bad-redaction detector. Verified at the Phase 7 research
# gate (skills/redaction-check/references/prior-art.md) to install clean without
# moving the pinned numpy/pandas/torch stack. x-ray pins `PyMuPDF==1.24.14` (fitz)
# as its own hard dep — PyMuPDF is AGPL-3.0, but as a DECLARED pip dependency the
# user installs (never vendored, never bundled as a binary) it does NOT infect
# Magpie's MIT license (prior-art §5). PyMuPDF does not depend on numpy/pandas, so
# the pins stay put. The other layered checks use pikepdf + pdfminer.six (already
# present via ocrmypdf); only box-over-text needs x-ray, lazily imported.
x-ray==0.3.6

# Phase 9 archive-evidence (scripts/evidence.py) -- RFC 3161 trusted timestamping.
#   rfc3161-client -- Apache-2.0; builds/parses/verifies a TimeStampReq/Resp. Its ONLY
#                     runtime dep is `cryptography` (already pulled + pinned by the
#                     stack above); ships a prebuilt cp39-abi3 win_amd64 wheel (no Rust
#                     toolchain) and does NO network -- transport is our job via
#                     `requests` (already present). Verified at the Phase 9 research
#                     gate (skills/archive-evidence/references/prior-art.md), incl. a
#                     live freeTSA round-trip + an `openssl ts -verify` cross-check.
rfc3161-client==1.0.6

# Phase 12 entity-extract (Track B) -- GLiNER entity NER + GLiREL relation extraction.
# Heavy ML, CPU-only, `gliner`-marked tests; weights download lazily on first use.
#   gliner -- zero-shot NER (model urchade/gliner_medium-v2.1, Apache-2.0, ~781 MB).
#             Apache-2.0 package.
#   glirel -- zero-shot RELATION extraction (model jackboyla/glirel-large-v0, ~1.87 GB).
#             The PACKAGE is Apache-2.0 but the WEIGHTS are CC BY-NC-SA 4.0
#             (NON-COMMERCIAL) -- adopted + documented (Tim's call); the user downloads
#             the weights, Magpie's MIT code is unaffected (the Phase-7 AGPL posture).
#   loguru -- glirel imports it but FORGETS to declare it; pinned explicitly here.
#   transformers / typer -- PINNED to the GLiNER<->Docling coexistence overlap. GLiNER
#             needs transformers >=4.51.3,<5.2; docling-ibm-models needs transformers
#             !=5.0/5.1/5.2/5.3. For typer: docling-core needs <0.25, BUT the docling
#             2.97.0 metapackage pulls docling-slim[standard]==2.97.0 whose `standard`
#             extra caps typer at <0.22 (>=0.12.5) -- the BINDING bound the earlier comment
#             missed. typer==0.24.2 therefore broke a fresh `pip install -r requirements-dev.txt`
#             at resolution (ResolutionImpossible); re-pinned to 0.21.2 (latest <0.22) to
#             satisfy the standard extra. transformers 4.57.6 stays the clean overlap
#             (gliner/glirel import; the docling heavy suite passes). WITHOUT the transformers
#             pin a rebuild resolves transformers 5.1.x (gliner's default), which
#             docling-ibm-models excludes. numpy/pandas/torch/spacy pins UNTOUCHED.
#   NOTE: the FtM mapping stack (followthemoney + nomenklatura) is LINUX/CI ONLY and
#   lives in requirements-ftm.txt (PyICU has no Windows wheel) -- NOT here.
transformers==4.57.6
typer==0.21.2
gliner==0.2.26
glirel==1.2.1
loguru==0.7.3
